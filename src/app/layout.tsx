import type { Metadata } from 'next';
import { Inter } from 'next/font/google';
import './globals.css';
import Header from '@/components/Header';
import Footer from '@/components/Footer';

const inter = Inter({ subsets: ['latin'] });

export const metadata: Metadata = {
  title: 'Boteju Enterprises - Quality Japanese Vehicles',
  description: 'Clear Prices. Clean Deals. Quality Rides. Import quality Japanese vehicles directly from Japan with transparent pricing and professional service.',
  keywords: 'japanese cars, car import, japan import, used cars, toyota, honda, nissan',
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        <Header />
        <main className="min-h-screen">
          {children}
        </main>
        <Footer />
      </body>
    </html>
  );
}